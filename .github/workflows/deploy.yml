name: 🚀 Deploy automático do FutZone (limpo e otimizado)

on:
  push:
    branches:
      - main  # Executa o deploy sempre que você fizer push na branch principal

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Baixa o código do repositório
      - name: 📦 Checkout do repositório
        uses: actions/checkout@v4

      # 2️⃣ Configura o Node.js
      - name: ⚙️ Instalar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # 3️⃣ Instala dependências
      - name: 📥 Instalar dependências
        run: npm install

      # 4️⃣ Gera o build de produção (pasta dist)
      - name: 🏗️ Gerar build
        run: npm run build

      # 5️⃣ Limpa branch gh-pages antiga antes do deploy
      - name: 🧹 Limpar branch gh-pages antiga
        run: |
          git fetch origin gh-pages || true
          git branch -D gh-pages || true
          git push origin --delete gh-pages || true

      # 6️⃣ Faz o deploy automático no GitHub Pages
      - name: 🚀 Publicar no GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
          user_name: LERACHI
          user_email: leandro@users.noreply.github.com
